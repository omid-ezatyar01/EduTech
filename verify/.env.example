# VITE_API_URL is shared with the other clients and may include /api/v1.
VITE_API_URL=http://localhost:5000/api/v1

# Optional override; the default is /api/v1/certificates/verify.
VITE_VERIFY_ENDPOINT=/api/v1/certificates/verify
